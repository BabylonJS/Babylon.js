# @babylonjs/gui-closure

Closure Compiler compatible build of `@babylonjs/gui`.

Use it with `@babylonjs/core-closure` and alias `@babylonjs/gui` to `@babylonjs/gui-closure` in your bundler. The package includes its Closure extern declarations automatically. Normal Babylon.js applications should continue using `@babylonjs/gui`.
