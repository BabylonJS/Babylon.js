/**
 * The state of a sound.
 */
export enum SoundState {
    /**
     * The sound is stopped.
     */
    Stopped,
    /**
     * The sound is playing.
     */
    Playing,
    /**
     * The sound is paused.
     */
    Paused,
}
